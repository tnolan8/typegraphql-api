import { DMMF, DataSource, GeneratorConfig } from '@prisma/generator-helper';
export interface ConfigMetaFormat {
    datasources: DataSource[];
    generators: GeneratorConfig[];
}
export declare type GetDMMFOptions = {
    datamodel?: string;
    cwd?: string;
    prismaPath?: string;
    datamodelPath?: string;
    retry?: number;
};
export declare function getDMMF({ datamodel, cwd, prismaPath, datamodelPath, retry, }: GetDMMFOptions): Promise<DMMF.Document>;
export declare function getConfig({ datamodel, cwd, prismaPath, datamodelPath, }: GetDMMFOptions): Promise<ConfigMetaFormat>;
export declare function getVersion(enginePath?: string): Promise<string>;
